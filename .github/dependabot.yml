version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Supply-chain mitigation: skip releases under a week old, so a hijacked or bad action tag is caught first.
    cooldown:
      default-days: 7
    groups:
      github-actions:
        patterns:
          - '*'
